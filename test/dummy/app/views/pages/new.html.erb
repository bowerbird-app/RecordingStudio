<% content_for :title, "New Page" %>

<div class="max-w-2xl">
  <h2 class="text-2xl font-semibold">New Page</h2>
  <p class="mt-1 text-sm text-slate-500">Create a new recording and first snapshot.</p>

  <div class="mt-6 rounded-lg border border-slate-200 bg-white p-6">
    <%= render "form", recordable: @recordable, form_url: pages_path, form_method: :post, submit_label: "Create Page" %>
  </div>
</div>
