<% content_for :title, "Config" %>

<%= render FlatPack::Breadcrumb::Component.new(class: "mb-4") do |breadcrumb| %>
  <% breadcrumb.item(text: "Home", href: root_path) %>
  <% breadcrumb.item(text: "Config") %>
<% end %>

<%= render FlatPack::PageTitle::Component.new(
  title: "Config",
  subtitle: "Current RecordingStudio settings for the sandbox app, annotated so the initializer behavior is easy to scan.",
  class: "mb-6"
) %>

<%= render FlatPack::CodeBlock::Component.new(
  code: @configuration_code,
  language: :ruby,
  title: "recording_studio.rb",
  separated: false
) %>
