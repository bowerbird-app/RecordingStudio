<% content_for :title, "Tree Methods" %>

<%= render FlatPack::Breadcrumb::Component.new(class: "mb-4") do |breadcrumb| %>
  <% breadcrumb.item(text: "Home", href: root_path) %>
  <% breadcrumb.item(text: "Methods", href: methods_path) %>
  <% breadcrumb.item(text: "Tree") %>
<% end %>

<%= render FlatPack::PageTitle::Component.new(
  title: "Tree",
  subtitle: "Reference examples for navigating the RecordingStudio recording tree from any recording node. Tree helpers return recordings, ids, or subtree relations depending on the method.",
  class: "mb-6"
) %>

<%= render "shared/method_catalog", method_catalog: @method_catalog %>