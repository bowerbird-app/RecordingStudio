<% content_for :title, "Access denied" %>

<%= render FlatPack::Card::Component.new(style: :elevated, class: "mt-10") do |card| %>
  <% card.header(divider: false) do %>
    <div class="space-y-2">
      <h1 class="text-2xl font-semibold">Access denied</h1>
      <p class="text-sm text-slate-500">You don’t have permission to view this page.</p>
    </div>
  <% end %>

  <% card.body do %>
    <div class="flex flex-wrap items-center gap-2">
      <%= render FlatPack::Button::Component.new(text: "Go back", style: :secondary, url: (request.referer.presence || root_path)) %>
      <%= render FlatPack::Button::Component.new(text: "Home", style: :primary, url: root_path) %>
    </div>
  <% end %>
<% end %>
