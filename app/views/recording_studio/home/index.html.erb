<div class="mx-auto max-w-3xl space-y-6 py-12">
  <div>
    <p class="text-sm uppercase tracking-wide text-slate-500">RecordingStudio Engine</p>
    <h1 class="text-3xl font-semibold text-slate-900">Recordings, Recordables, Events</h1>
    <p class="mt-2 text-sm text-slate-600">
      This engine provides a root recording API for creating immutable recordable snapshots
      and an append-only event timeline. Mount it inside your host app and customize via the
      RecordingStudio initializer.
    </p>
  </div>

  <div class="rounded-lg border border-slate-200 bg-white p-6 text-sm text-slate-600">
    <p class="font-semibold text-slate-900">Next steps</p>
    <ul class="mt-3 list-disc space-y-2 pl-5">
      <li>Run <code class="rounded bg-slate-100 px-2 py-1 text-xs">rails g recording_studio:migrations</code> to install tables.</li>
      <li>Register recordable types in <code class="rounded bg-slate-100 px-2 py-1 text-xs">config/initializers/recording_studio.rb</code>.</li>
      <li>Create a root Recording per top-level recordable and call recording APIs from that root.</li>
    </ul>
  </div>
</div>
