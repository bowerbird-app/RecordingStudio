<div class="mx-auto max-w-3xl space-y-6 py-12">
  <div>
    <p class="text-sm uppercase tracking-wide text-slate-500">ControlRoom Engine</p>
    <h1 class="text-3xl font-semibold text-slate-900">Recordings, Recordables, Events</h1>
    <p class="mt-2 text-sm text-slate-600">
      This engine provides a container-first API for creating immutable recordable snapshots
      and an append-only event timeline. Mount it inside your host app and customize via the
      ControlRoom initializer.
    </p>
  </div>

  <div class="rounded-lg border border-slate-200 bg-white p-6 text-sm text-slate-600">
    <p class="font-semibold text-slate-900">Next steps</p>
    <ul class="mt-3 list-disc space-y-2 pl-5">
      <li>Run <code class="rounded bg-slate-100 px-2 py-1 text-xs">rails g control_room:migrations</code> to install tables.</li>
      <li>Register recordable types in <code class="rounded bg-slate-100 px-2 py-1 text-xs">config/initializers/control_room.rb</code>.</li>
      <li>Use <code class="rounded bg-slate-100 px-2 py-1 text-xs">ControlRoom::HasRecordingsContainer</code> in your container models.</li>
    </ul>
  </div>
</div>
