# RecordingStudio Installed

Next steps:

1. Run `bin/rails db:migrate`.
2. Review `config/initializers/recording_studio.rb`.
3. If your app uses Tailwind, rebuild assets with `bin/rails tailwindcss:build`.
