===============================================================================

RecordingStudio has been installed successfully!

The engine has been mounted at /recording_studio in your application.

Review config/initializers/recording_studio.rb to register recordable types and
customize actor, idempotency, and instrumentation settings.

Install migrations with:
  bin/rails generate recording_studio:migrations

If you use Tailwind CSS:
1. Run 'bin/rails tailwindcss:build' to rebuild your CSS with RecordingStudio styles

To use the engine:
1. Start your Rails server
2. Visit http://localhost:3000/recording_studio

===============================================================================
